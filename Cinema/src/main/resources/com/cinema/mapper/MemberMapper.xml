<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.cinema.mapper.MemberMapper">
 	<insert id="join">
        insert into tbl_member(mem_userid, mem_password, mem_username, mem_nickname, mem_email, mem_phone) values (#{mem_userid}, #{mem_password}, #{mem_username}, #{mem_nickname}, #{mem_email}, #{mem_phone})
    </insert> 
  </mapper>